using Entities;
using DAO;

namespace Models
{
    public class ManagerAuthorization
    {
        /*public Response ValidateAccess (InValidateAccess input)
        {
            InValidateAccess userData = new InValidateAccess();
            AuthorizationDAO dao = new AuthorizationDAO();
            userData.company = "1";
            userData.userID = input.userName;
            userData.controlName 
            return dao.ValidateAccess(input);
        }*/
    }
}
